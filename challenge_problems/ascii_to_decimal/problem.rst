Assume that your code gets an hex-string in the input and returns a decimal value which must
be equal to the value that the hex-string represents. For example, "A0" and "A000" must return
decimal values of "160" and "4090", respectively.

Note: the input should always be given with two digits representing one byte.